"use strict";// Treat all JS code as newer version.

//alert(3+3) //On browser it will work but we are using node js so, it will not work right now.

console.log(3+
    3); // code readability should be high.

/* DATATYPES IN JS


number=> Range is 2 to the power 53
bigint=> for large data or numbers we use
string
boolean=> true/fasle
null=> a standalone value (It represents an empty value.)
undefined=> value not defined
symbol=> for finding the uniqueness we use symbol.
object=>  
typeof()=>
*/

let age=23;
console.log(typeof age)

/* INTERVIEW QUESTION
console.log(typeof null) //Returns an object
console.log(typeof undefined); // Returns undefined 
*/
